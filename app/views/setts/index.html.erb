<h1> Settings </h1> 
<% @setts.each do |s| %>
    <li><%= link_to s.name, project_sett_path(params[:project_id], s) %></li>
<% end %> 
<%= link_to 'Add Setting', new_project_sett_path(params[:project_id]) %></br>
<%= link_to 'Back to Project', project_path(params[:project_id])%>