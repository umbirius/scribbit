<h1>Characters</h1> 
<% @characters.each do |c| %> 
    <li><%= link_to c.name, project_character_path(params[:project_id],c) %></li>
<% end %> 

<%= link_to 'Add Character', new_project_character_path(params[:project_id]) %></br>
<%= link_to "Back to #{@project.title} page", project_path(params[:project_id])%>